// Moved to the @nwa/task-kernel package. This re-export keeps existing
// `../services/FileSystemService` imports working during the monorepo migration.
export { FileSystemService } from '@nwa/task-kernel';
